version: 2.1
orbs:
  gradle: circleci/gradle@3.0
executors:
  java17:
    docker:
      - image: 'amazoncorretto:17'  

jobs:
  install-shasum:
    executor: java17
    steps:
    - run: |
        apt-get install -y shasum
workflows:
  build:
    jobs:
      - install-shasum
      - gradle/test:
          executor: java17
